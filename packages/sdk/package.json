{
  "name": "@hsp/sdk",
  "version": "0.1.0",
  "description": "HSP developer SDK — payer-side one-call pay() orchestration (sign → register → wallet-broadcast → observe → await) and merchant-side independent verification with a pinned policy.",
  "private": true,
  "license": "Apache-2.0",
  "type": "module",
  "exports": {
    ".": "./src/index.ts",
    "./*": "./src/*.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@hsp/core": "*",
    "viem": "2.52.2"
  },
  "devDependencies": {
    "@types/node": "22.19.17",
    "tsx": "4.21.0",
    "typescript": "5.9.3"
  }
}
