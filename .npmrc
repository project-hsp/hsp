# Supply-chain hardening: never run install lifecycle scripts (postinstall etc.).
# Explicit `npm run <script>` still works (only its pre/post hooks are skipped).
# A native dep that genuinely needs its install step (better-sqlite3) gets an
# explicit, reviewed `npm rebuild better-sqlite3 --foreground-scripts` afterwards.
ignore-scripts=true
